export const UI_MOTION = {
  themeTransitionMs: 600,
  panelFadeDuration: 0.22,
  iconSwitchDuration: 0.18,
  loadingShimmerSeconds: 1.2,
} as const;

export const EDITOR_MOTION = {
  revealStaggerMs: 50,
  revealDurationMs: 240,
} as const;
